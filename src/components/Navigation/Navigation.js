import React from 'react';

const Navigation = () => {
    return (
        <div style={{display: 'flex', justifyContent: 'flex-end'}}>
            <p className='dim underline f3 pa3 link black pointer'>Sign Out</p>
        </div>
    );
}

export default Navigation;